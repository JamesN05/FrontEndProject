import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CorkCobhMidletonPage } from './cork-cobh-midleton.page';

describe('CorkCobhMidletonPage', () => {
  let component: CorkCobhMidletonPage;
  let fixture: ComponentFixture<CorkCobhMidletonPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CorkCobhMidletonPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
