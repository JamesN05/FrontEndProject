import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DundalkBelfastPage } from './dundalk-belfast.page';

describe('DundalkBelfastPage', () => {
  let component: DundalkBelfastPage;
  let fixture: ComponentFixture<DundalkBelfastPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DundalkBelfastPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
