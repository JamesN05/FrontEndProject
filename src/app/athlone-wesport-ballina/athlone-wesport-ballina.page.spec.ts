import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AthloneWesportBallinaPage } from './athlone-wesport-ballina.page';

describe('AthloneWesportBallinaPage', () => {
  let component: AthloneWesportBallinaPage;
  let fixture: ComponentFixture<AthloneWesportBallinaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AthloneWesportBallinaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
